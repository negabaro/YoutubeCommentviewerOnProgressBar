import { Istate } from '@/types/store';

export default {
  currentNextPageToken: null,
  // allCommentCount: 0,
  // allFilteredCommentCount: 0,
  slideBarWidthSize: 0,
  currentVideoId: '',
  // videoDuration: 0,
} as Istate;
